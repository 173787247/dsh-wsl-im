# Contributing

1. Keep secrets out of tool returns and README examples (use env files).
2. Prefer porting protocols from OryxOS channel adapters (`docs/PROTOCOL.md`) over inventing new ones.
3. Do not add a runtime dependency on OryxOS HTTP.
4. Run `npm test` before opening a PR.
