# Contributing

1. Keep secrets out of tool returns and README examples (use env files).
2. Implement wire behavior from vendor protocol docs (`docs/PROTOCOL.md`).
3. Do not commit secrets or vendored third-party trees.
4. Run `npm test` before opening a PR.
